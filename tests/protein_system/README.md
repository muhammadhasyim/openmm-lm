# Protein cavity tests

Verification tests for the protein cavity workflow. Simulation scripts live under
[`examples/cavity/protein_system/`](../../examples/cavity/protein_system/).

Run tests from the repository root:

```bash
pixi run -e test python -m pytest tests/protein_system/test_protein_cavity.py -q
```
